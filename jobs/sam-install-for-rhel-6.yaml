- job-template:
    name: 'sam-install-for-rhel-6-provision'
    defaults: virt-who-provision
    node: master
    builders:
        - shell: |
            #!/bin/bash

            kinit -k -t $KEYTAB $PRINCIPAL

            export JSLAVENAME={jslavename}
            # Provision Jenkins Slave
            if [ "$JSLAVENAME" != "master" ]
            then
                $WORKSPACE/ci-ops-central/bootstrap/provision_jslave.sh --site=$SITE --project_defaults={project_defaults} \
                --topology={topology_path}/ent_jslave --ssh_keyfile={ssh_keyfile} \
                --jslavename={jslavename} --jslaveflavor={jslaveflavor} --jslaveimage={jslaveimage} \
                --jslave_execs={jslave_execs} --jslavecreate --resources_file={jslavename}.json

                TR_STATUS=$?
                if [ "$TR_STATUS" != 0 ]; then echo "ERROR: Provisioning\nSTATUS: $TR_STATUS"; exit 1; fi
            fi

            # Provision Test Resources
            $WORKSPACE/ci-ops-central/bootstrap/provision_resources.sh --site=$SITE --project_defaults={project_defaults} \
            --topology={topology_path}/{topology} --ssh_keyfile={ssh_keyfile} --name=sam-install-for-rhel-6

            TR_STATUS=$?

            files=$(ls $WORKSPACE/*.slave 2>/dev/null)
            if [ -e "$files" ]
            then
                cat $WORKSPACE/*.slave >> $WORKSPACE/RESOURCES.txt
            fi

            if [ "$TR_STATUS" != 0 ]; then echo "ERROR: Provisioning\nSTATUS: $TR_STATUS"; exit 1; fi

        - inject:
            properties-file: $WORKSPACE/RESOURCES.txt

    publishers:
      - archive:
          artifacts: '*.txt, *.json'
          allow-empty: 'true'
      - trigger-parameterized-builds:
          - project: 'sam-install-for-rhel-6-runtest'
            current-parameters: true
            condition: 'SUCCESS'
            property-file: $WORKSPACE/RESOURCES.txt
            fail-on-missing: true
      - default-virt-who-publishers

- job-template:
    name: 'sam-install-for-rhel-6-runtest'
    defaults: virt-who-runtest
    node: '{jslavename}'
    builders:
        - copyartifact:
            project: 'sam-install-for-rhel-6-provision'
            filter: '*.txt, *.json'
            target: $WORKSPACE

        - shell: |
            #!/bin/bash
            set -x

            export TOPOLOGY={topology}
            {testparams}

            echo "TOPOLOGY: {topology}"

            echo "Ping Jenkins Slave"
            ping -c 10 $JSLAVEIP

            echo "Jenkins machine info we are running on"
            ifconfig

            echo "Pinging Test Resources"
            echo $EXISTING_NODES | xargs -i -d , ping -c 15 {{}}
            cat $WORKSPACE/RESOURCES.txt

            beaker_ip=`ping $EXISTING_NODES -c 1 | awk -F '[()]' 'NR==2' | cut -d '(' -f 2 | cut -d ')' -f 1`
            export REMOTE_IP=$beaker_ip

            pushd $WORKSPACE/entitlement-ci/testcases/sam_install
            nosetests sam_install.py --with-xunit --xunit-file=$WORKSPACE/nosetests.xml
            popd

            echo SAM_IP=$beaker_ip > $WORKSPACE/SAMSERVER.txt
            echo SAM_HOSTNAME=$EXISTING_NODES >> $WORKSPACE/SAMSERVER.txt

        - inject:
            properties-file: $WORKSPACE/SAMSERVER.txt

    publishers:
      - archive:
          artifacts: '**/**'
          allow-empty: 'true'
      - default-virt-who-runtest-publishers
      - trigger-parameterized-builds:
          - project: 'virt-who-esx-rhel-6-provision'
            current-parameters: false
            condition: 'SUCCESS'
            property-file: $WORKSPACE/SAMSERVER.txt
            fail-on-missing: true
          - project: 'virt-who-kvm-rhel-6-provision'
            current-parameters: false
            condition: 'SUCCESS'
            property-file: $WORKSPACE/SAMSERVER.txt
            fail-on-missing: true
          - project: 'rhsm-level-1-rhel-6-provision'
            current-parameters: false
            condition: 'SUCCESS'
            property-file: $WORKSPACE/SAMSERVER.txt
            fail-on-missing: true
          - project: 'rhsm-gui-rhel-6-provision'
            current-parameters: false
            condition: 'SUCCESS'
            property-file: $WORKSPACE/SAMSERVER.txt
            fail-on-missing: true
      - default-virt-who-publishers

- job-group:
    name: sam-install-for-rhel-6-provision-runtest
    jobs:
      - 'sam-install-for-rhel-6-provision'
      - 'sam-install-for-rhel-6-runtest'

- project:
    name: sam-install-for-rhel-6-jobs
    project:
     - sam-install-for-rhel-6
    project_defaults:
     - entitlement-ci/config/project_defaults
    topology_path:
     - entitlement-ci/config
    topology:
     - bkr_sam_install
    ssh_keyfile:
     - entitlement-ci/config/keys/ent-key
    tests_path:
     - entitlement-ci/tests
    testparams:
     - echo "I am a test parameter"
    jobs:
      - sam-install-for-rhel-6-provision-runtest
    jslavename:
     - jslave-sam-install-for-rhel-6-slave
    jslave_execs:
     - 10
    jslaveimage:
     - RHEL-6.6-Server-x86_64-released
    jslaveflavor:
     - m1.medium
    jslaveusername:
     - root
    jslavepassword:
     - redhat
    jslaveteardown:
     - True